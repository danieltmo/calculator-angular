import { HighlightValueDirective } from './highlight-value.directive';

describe('HighlightValueDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightValueDirective();
    expect(directive).toBeTruthy();
  });
});
